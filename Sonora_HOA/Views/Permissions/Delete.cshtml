@model CostaDiamante_HOA.Models.Permissions

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
    string ownerID = ViewBag.condoID;
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Permissions</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.condo.name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.condo.name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.startDate)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.startDate)
        </dd>

        <dt>
            @*@Html.DisplayNameFor(model => model.guest.name)*@
            Guest's Name
        </dt>

        <dd>
            @Html.DisplayFor(model => model.guest.fullName)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-danger" /> |
            @Html.ActionLink("Back to List", "Create", "Visits", new { id = ownerID }, null)
        </div>
    }
</div>
