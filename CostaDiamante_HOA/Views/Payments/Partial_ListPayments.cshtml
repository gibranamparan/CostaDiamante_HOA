@*@model IEnumerable<CostaDiamante_HOA.Models.Payments>*@
@using CostaDiamante_HOA.Models

@*<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.owner.name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.visit.ownerID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.amount)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.typeOfPayment)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.owner.name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.visit.ownerID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.amount)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.typeOfPayment)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.paymentsID }) |
            @Html.ActionLink("Details", "Details", new { id=item.paymentsID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.paymentsID })
        </td>
    </tr>
}

</table>*@


<div id="app">
    <h1>{{ message }}</h1>
    <ul>
        <li v-for="item in payments">{{ item }}</li>
    </ul>
</div>
@Scripts.Render("~/node_modules/vue/dist/vue.min.js")

<script>
    const v = new Vue({
        el: '#app',
        data: {
            message: 'Hello Vue.js!',
            payments: this.$data
        }
    })
</script>

@*@foreach (var d in Model)
{
    @:this.payments.push("@d");
}*@